\section{Background}

\subsection{MLPs and Layered Function Approximation}

MLPs can be described as a composition of affine transformations and (typically) non linear activation functions \cite{cybenko1989approximation,hornik1989multilayer}.
Iterations on derivative architectures modify this basic pattern with tricks like weight sharing, recurrence, attention blocks, or structured sparsity.
However, they still maintain the feedforward directionality of the network.

Moreover, standard dense MLPs are not able to encode more complex inductive biases such as rotational invariance or translational invariance by themselves.
These biases must be introduced through some creative license on network features like architecture, data representation, or training procedure.

HexNNs, as introduced in this paper, keeps this adjacent-layer bipartite structure from MLPs but changes its own topology to allow for multiple feedforward interpretations from the same graph, potentially allowing for more complex inductive biases.

%%% SUGGESTION: Existing text: "more complex inductive biases"
%%% SUGGESTION: This is reasonable but still broad. Consider explicitly naming which biases are hypothesized rather than implied.

\subsection{Symmetry and Equivariance in Neural Networks}

Convolutional Neural Networks (CNNs) are a prime example of how the architural constraints of the input domain can be used to encode more complex inductive biases \cite{lecun1998gradient,fukushima1980neocognitron}.
They encode translational equivariance and locality of features in the input data by using convolutional filters that are "slid" across the input data.
Their derivative architecture use geometric iterations on their kernels and inputs to help them unlock higher level inductive biases.
Group-equivariant CNNs encode the symmetry of the input data itself to achieve rotation, reflection, or other group equivariances \cite{cohen2016group,worrall2017harmonic}.
Capsule Networks project their input data into higher dimensionality with vector-style representations and model part-whole relationships \cite{sabour2017dynamic}.

All of these architectures, however, still impose a feed forward directionality on the layer ordering.
In the case of HexNNs, the symmetry is imposed on the network topology itself, not only the input domain or filter group.
This allows for the same node/weight structure to be reinterpreted directionally with abstraction bleeding between the different directions.

%%% SUGGESTION: Existing text: "abstraction bleeding"
%%% SUGGESTION: This wording is evocative but not technically precise.
%%% SUGGESTION: Consider replacing with something like:
%%% SUGGESTION: "cross-orientation parameter coupling"
%%% SUGGESTION: or
%%% SUGGESTION: "shared representational influence between orientations"

\subsection{Graph Neural Networks}

Graph neural networks operate on data whose structure is represented by a graph \cite{scarselli2009graph,gilmer2017neural}.
In message-passing GNNs, node representations are updated by aggregating information from neighboring nodes according to an adjacency relation.

HexNNs also use an adjacency structure, but the present formulation differs from standard GNNs in two ways.
First, the graph defines the architecture itself rather than the input data.
Second, computation proceeds through orientation-specific feedforward traversals rather than repeated permutation-invariant neighborhood aggregation.
For this reason, the current HexNN implementation is better understood as a graph-structured or sparse MLP topology rather than a conventional message-passing GNN.

%%% SUGGESTION: This subsection is important defensively.
%%% SUGGESTION: Reviewers will likely ask whether HexNNs are simply GNNs with fixed adjacency.
%%% SUGGESTION: The distinction between "graph as architecture" vs "graph as data" is a strong clarification.

\subsection{Parameter Sharing, Ensembles, and Multi-Head Models}

Parameter sharing is commonly used to reduce model size and impose structure \cite{caruana1997multitask}.
CNNs share kernels across spatial locations, recurrent networks reuse transition parameters across time, and multi-task models often share a representation backbone across related objectives.
HexNNs use a different form of sharing: orientation-specific subnetworks are coupled through a common underlying graph.
Training one orientation may therefore alter the behavior of other orientations, creating measurable transfer or interference.

HexNNs can also be compared with ensembles.
Unlike an ensemble of independent models, a HexNN does not maintain separate parameters for each orientation.
Its directional subnetworks are not independent; their behavior is coupled by construction.

%%% SUGGESTION: Existing text: "can also be compared with ensembles"
%%% SUGGESTION: This comparison is probably safer than claiming MoE behavior.
%%% SUGGESTION: Deep ensembles, snapshot ensembles, SWA, and shared-backbone multihead systems are probably your best comparison family.

\subsection{Continual Learning and Interference}

Sequential learning can produce interference between tasks through shared parameters \cite{kirkpatrick2017overcoming}.
This is commonly discussed through the lens of catastrophic forgetting, where optimizing for a later task degrades performance on previously learned tasks.

The present work is not a continual-learning system in the traditional sense.
However, HexNN orientations may still interfere with one another because they share parameters through the same underlying topology.
The relevant connection is therefore parameter interference rather than task identity.

%%% SUGGESTION: This version is substantially safer than directly analogizing to human cognition.

\subsection{Cognitive Motivation}

Foundational neural-network architectures have historically drawn inspiration from biological systems.
CNNs were partially motivated by the visual cortex and locality-sensitive receptive fields \cite{fukushima1980neocognitron,lecun1998gradient}.
Recurrent architectures and LSTMs introduced mechanisms resembling persistent internal state and temporal feedback \cite{hochreiter1997long}.

The HexNN structure was partly motivated by questions about compartmentalized behavior and shared internal representation.
To borrow speculative language from Douglas Hofstadter, one can imagine a network topology capable of maintaining multiple directional interpretations of the same underlying substrate \cite{hofstadter1979geb,hofstadter2007strange}.

%%% SUGGESTION: Existing text in prior draft heavily emphasized DID.
%%% SUGGESTION: Keeping the cognitive framing heuristic and lightweight is probably the safer conference-paper choice.
%%% SUGGESTION: Reviewers in ML venues may react negatively if DID analogies appear central to the scientific claim.

This paper does not model DID or attempt to reproduce human cognitive conditions.
The analogy is heuristic rather than empirical and primarily serves as motivation for exploring parameter sharing, directional reinterpretation, and interference between coupled subnetworks.

%%% SUGGESTION: Existing autobiographical material about family medical history is probably too personal and not scientifically useful for the conference version.
%%% SUGGESTION: It may fit better in acknowledgements, talks, or a future reflective writeup rather than the main paper.
