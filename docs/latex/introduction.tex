\section{Introduction}

\subsection{Architectural Directionality as an Inductive Bias}

Multilayer Perceptrons (MLPs) are traditionally structured as a unidirectional chain of connected layers, where each layer sequentially connects to the next.
This imposes a simple ordered sequence of transformations on the input data, where each layer defines a projection from one vector space to another, along with some activation function.
Hidden layers can then be interpreted as intermediate representations whose structure depends on the imposed ordering of the network.

Through these variations, many inductive biases have been successfully encoded such as convolutional locality, invariance to translation, rotation, scaling, and other geometric transformations.
While many variations of this base model have been iterated on with respect to shared weights, activation functions, and other hyperparameters, the core architectural ordering of the network remains the same and only a few deviations encode geometric transformations of the data, such as 2D or 3D convolution layers popularized in computer vision tasks.
Convolutional Neural Networks (CNNs) encode locality and translation equivariance through shared convolutional kernels. Extensions such as group-equivariant CNNs impose additional transformation structure, including rotation or reflection equivariance, by tying parameters across transformations.

\subsection{Hexagonal Neural Networks}

Here, we introduce Hexagonal Neural Networks (HexNNs) that generalizes MLPs into a rotationally symmetric, multi-directional structure.

This architecture arranges nodes in a two-dimensional hexagonal topology rather than a single ordered layer stack, like the example shown in Figure \ref{fig:hexnet_n3_r0}.
This allows for the same set of nodes and edges to be interpreted and traversed under multiple directed orientations.
Thusly, it is now possible to train one orientation while evaluating how that update affects the other orientations.
This yields a central question: whether this directional weight sharing produces measurable transfer or interference between orientations.

% Here, we introduce Hexagonal Neural Networks (HexNNs) that generalizes MLPs into a rotationally symmetric, multi-directional structure.

% HexNNs replace the standard 1D layer stack with an initial 2D topological arrangement made to resemble regular hexagons.
% In any one direction, they may still be intepreted as a unidirectional chain of connected layers.
% This allows for the same node set may be traversed in a different direction, and the network may be interpreted as a different network in that direction with some overlap of weights between the two networks.

Because each orientation exposes input and output layers of equal width, the present formulation is most natural for tasks where input and output vectors have the same dimensionality, such as reconstruction, identity mappings, or vector-to-vector regression.
This would not necessarily be well suited for machine learning tasks such as classification that require a fixed input and output space, although future work may explore this further.

Given a size parameter, $n$, the network has equal $n$ length input and output layers, with hidden layers symmetrically increasing and decreasing to $2n-1$.
Crucially, each node in layer $i$ is connected to all nodes in layer $i+1$, but unlike MLPs, this bipartite adjacency is true for all three cardinal directions of a hexagon.
This allows for rotation-based reinterpretation of the same input data.

This design enables six distinct orientations of the same network, each corresponding to a $60^\circ$ rotation of the hexagonal structure.
We index orientations as $H_0, ..., H_5$ starting with the default orientation $H_0$ being the top-to-bottom orientation.
The opposite orientation of $H_i$ is $H_{(i+3) \bmod 6}$.

\begin{figure}
    \includegraphics[width=1\linewidth]{hexnet_n3_r0_structure.png}
    \caption{A Hexnet with N=3 and default rotation $H_0$}
    \label{fig:hexnet_n3_r0}
\end{figure}

\subsection{Open Questions}

As we can see, this new change in architecture may have some interesting implications for the training of the network.
What are the implications of training the network in one direction, and how does that impact the training of the network in other directions?
What effects will this have on the performance of the network in other directions?
Is there an optimal sequencing of training with intermitten rotations that can yield lower overall loss in any one network or a mixture of experts (MoE) approach that can yield lower overall loss than the MLP equivalent?

\subsection{Contributions}

In this paper, we present a memory efficient implementation using over padded state vectors and orientation specific adjacency matrices.
We also establish baselines for HexNNs and their equivalent MLPs and compare the performance of the two architectures across various datasets and hyper parameter configurations.

% todo: we don't have this yet
We also present the inductive bias: [blank]
Unlike MLPs, HexNNs are able to present [blank] which can tell us if the data is [blank].
This could be useful for [blank] tasks where modern day neural networks are not able to perform well.

% This is because the network is now able to see the data from a different perspective, and therefore, it may be able to learn a different representation of the data.
% This is similar to the idea of transfer learning, where a network trained on one task is able to help train a network on a different task.
% However, in this case, the network is able to see the data from a different perspective, and therefore, it may be able to learn a different representation of the data.
% This is a new way of thinking about training neural networks, and it may be able to help us understand how to best train neural networks.

