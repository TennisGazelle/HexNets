"""HexNet Streamlit UI package (multipage sidebar + shared helpers).

CLI metadata: `hexnets_web.cli_types` (used by CLI and UI); aggregated root node in
`hexnets_web.pages.cli.cli_data` (`CLI_ROOT`).
"""
