# Benchmark families (index)

**Canonical doc:** [`docs/math/benchmark-families.md`](../docs/math/benchmark-families.md) — families A–F, iteration matrix, slates, insights tables, and how [`e2e_test.sh`](../e2e_test.sh) maps to them.

Do not treat files under `stories/` as ground truth for benchmarks or math; use `docs/math/` and `src/` instead.
